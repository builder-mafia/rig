import { summary as summaryPrompt } from './summary';
import { title as titlePrompt } from './title';

export namespace Prompt {
  export const title = titlePrompt;
  export const summary = summaryPrompt;
}
